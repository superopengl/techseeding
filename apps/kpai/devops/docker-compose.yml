services:
  app:
    build:
      context: ..
      dockerfile: devops/Dockerfile
    ports:
      - "80:80"
    env_file:
      - ../.env.production
    environment:
      KPAI_API_SERVICE_URL: ${KPAI_API_SERVICE_URL:-http://0.0.0.0:80}
      RUN_MIGRATIONS: "true"
